package com.dsa.stack;

import java.util.Stack;

public class Solution {
    public int maxWidthRamp(int[] nums) {
        int length = nums.length;
        int maxRamp = Integer.MIN_VALUE;
        

        if (maxRamp == Integer.MIN_VALUE) {
            return 0;
        }
        return maxRamp;
    }
}
